const getTheTitles = function(books) {
    return books.map(book => book.title)
    };

//it was fairly easy identifying what tool to use, however for whatever reason
//it was being extremely difficult in the implementation
//i guess i had the syntax minorly wrong and everytime i "fixed" it
//i messed something else up, oh well i got there eventually.

// Do not edit below this line
module.exports = getTheTitles;
